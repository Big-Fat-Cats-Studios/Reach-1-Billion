to start project:
1. ensure go packages are installed and nothing is unneeded `go mod tidy`
2. use command `go run .` to work locally
3. use command `go build .` to build (do this when its time to deploy)
